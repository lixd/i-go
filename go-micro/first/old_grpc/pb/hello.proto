syntax = "proto3";

package go.micro.srv.hello;

// 定义一个服务
service Hello {
    // 定义 SayHello 方法
    rpc SayHello (User) returns (Msg) {
    }
}

//  request
message User {
    string name = 1;
}

//  response
message Msg {
    string message = 1;
}

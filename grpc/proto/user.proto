syntax = "proto3";

package helloworld;
// 定义一个服务
service UserService {
    rpc Create (UserReq) returns (UserResp) {
    }
}
message UserReq {
    string name = 1;
    string age = 2;
}
message UserResp {
    string message = 1;
}
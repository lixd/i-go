syntax = "proto3";

package helloworld;

// 定义一个服务
service Hello {
    // 定义 SayHello 方法
    rpc SayHello (HelloReq) returns (HelloRep) {
    }
}

//  request
message HelloReq {
    string name = 1;
}

//  response
message HelloRep {
    string message = 1;
}
